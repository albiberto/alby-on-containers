@using ProductDataManager.Domain.Aggregates.DescriptionAggregate

<MudDataGrid T="DescriptionValue" Items="@Values" SortMode="SortMode.Multiple" Filterable="true" FilterMode="DataGridFilterMode.Simple">
    <Columns>
        <PropertyColumn Property="x => x.Name" Title="Value" Sortable="true" Filterable="true" />
    </Columns>
    <PagerContent>
        <MudDataGridPager T="DescriptionValue" />
    </PagerContent>
</MudDataGrid>